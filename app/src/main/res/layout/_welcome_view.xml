<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:layout_editor_absoluteX="0dp"
        tools:layout_editor_absoluteY="-114dp">

        <include layout="@layout/_background" />
    </RelativeLayout>

    <LinearLayout
        android:id="@+id/welcome_image_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="225dp"
        app:layout_constraintTop_toTopOf="parent">
        <ImageView
            android:id="@+id/welcome_image"
            android:layout_width="200dp"
            android:layout_height="176dp"
            android:background="@drawable/_welcome_image" />
    </LinearLayout>

    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="42dp"
        android:fontFamily="@font/poppins_semibold"
        android:gravity="center"
        android:text="Witamy w  [Nazwa apki] !"
        android:textSize="20sp"
        android:textStyle="normal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/welcome_image_section" />

    <TextView
        android:id="@+id/info_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="42dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:fontFamily="@font/poppins"
        android:gravity="center"
        android:text="Zarządzaj swoimi zajęciami i monitoruj postępy w jednym miejscu."
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/welcome_text" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:layout_marginBottom="70dp"
        app:layout_constraintBottom_toBottomOf="parent">


        <Button
            android:id="@+id/login_button"
            android:layout_width="346dp"
            android:layout_height="70dp"
            android:layout_marginBottom="31dp"
            android:background="@drawable/_red_big_button"
            android:fontFamily="@font/poppins_semibold"
            android:gravity="center"
            android:onClick="loginHandler"
            android:text="Logowanie"
            android:textAllCaps="false"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/return_button" />

        <Button
            android:id="@+id/register_button"
            android:layout_width="346dp"
            android:layout_height="70dp"
            android:background="@drawable/_green_big_button"
            android:fontFamily="@font/poppins_semibold"
            android:gravity="center"
            android:onClick="registerHandler"
            android:text="Rejestracja"
            android:textAllCaps="false"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>